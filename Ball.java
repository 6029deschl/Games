public class Ball {
	int x, y, z;

	// ball variable
	// starting position(x y z points)
	// kicked when mouse clicked
	// reset after decided if goal or not
	public void kickBall() {
		for (int i = z; i > 0; i--) {
			// ball moves forward
			// if(ball goes in goal)
			// reset();
		}
	}
}
